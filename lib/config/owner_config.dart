// TODO: Replace this with your actual Firebase Auth UID
// You can find this in the Firebase Console > Authentication > Users
// or in the browser console logs if you run the app (we log it in LoginScreen/Cloud Functions).
const String OWNER_UID = "REPLACE_WITH_YOUR_OWNER_UID";
